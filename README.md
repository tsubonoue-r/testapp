# testapp

**Miyabi**フレームワークによる自律型開発プロジェクト

## はじめに

### 前提条件

```bash
# 環境変数の設定
cp .env.example .env
# .env を編集してトークンを追加
```

### インストール

```bash
npm install
```

### 開発

```bash
npm run dev          # 開発サーバーを起動
npm run build        # プロジェクトをビルド
npm test             # テストを実行
npm run typecheck    # 型チェック
npm run lint         # コードをLint
```

## プロジェクト構造

```
testapp/
├── src/              # ソースコード
│   └── index.ts     # エントリーポイント
├── tests/           # テストファイル
│   └── example.test.ts
├── .claude/         # AIエージェント設定
│   ├── agents/      # エージェント定義
│   └── commands/    # カスタムコマンド
├── .github/
│   ├── workflows/   # CI/CD自動化
│   └── labels.yml   # ラベルシステム（53ラベル）
├── CLAUDE.md        # AI コンテキストファイル
└── package.json
```

## 🌸 Miyabi Framework

このプロジェクトは**7つの自律型AIエージェント**を使用しています:

1. **CoordinatorAgent** - タスク統括・並列実行制御
2. **IssueAgent** - Issue分析・自動ラベリング
3. **CodeGenAgent** - AI駆動コード生成
4. **ReviewAgent** - コード品質判定（80点以上で合格）
5. **PRAgent** - Pull Request自動作成
6. **DeploymentAgent** - CI/CDデプロイ自動化
7. **TestAgent** - テスト実行・カバレッジ計測

### ワークフロー

1. **Issueを作成**: 実装したい内容を記述
2. **Agentが作業**: AIエージェントが分析、実装、テスト
3. **PRをレビュー**: 生成されたPull Requestを確認
4. **マージ**: 自動デプロイ

### ラベルシステム

Issueは自動的に状態遷移します:

- `📥 state:pending` - エージェント割当待ち
- `🔍 state:analyzing` - 分析中
- `🏗️ state:implementing` - コード実装中
- `👀 state:reviewing` - レビュー中
- `✅ state:done` - 完了・マージ済み

## コマンド

```bash
# プロジェクト状態確認
npx miyabi status

# リアルタイム監視
npx miyabi status --watch

# 新しいIssueを作成
gh issue create --title "機能追加" --body "説明"
```

## 設定

### 環境変数

必須変数（`.env.example`を参照）:

- `GITHUB_TOKEN` - GitHub Personal Access Token
- `ANTHROPIC_API_KEY` - Claude APIキー（ローカル開発ではオプション）
- `REPOSITORY` - 形式: `owner/repo`

### GitHub Actions

ワークフローは`.github/workflows/`に事前設定されています:

- CI/CDパイプライン
- 自動テスト
- デプロイ自動化
- エージェント実行トリガー

**注**: リポジトリシークレットを以下で設定してください:
`https://github.com/tsubonoue-r/testapp/settings/secrets/actions`

必須シークレット:
- `GITHUB_TOKEN` (GitHub Actionsが自動提供)
- `ANTHROPIC_API_KEY` (エージェント実行のため手動追加)

## ドキュメント

- **Miyabi Framework**: https://github.com/ShunsukeHayashi/Miyabi
- **NPM Package**: https://www.npmjs.com/package/miyabi
- **ラベルシステム**: `.github/labels.yml`を参照
- **エージェント運用**: `CLAUDE.md`を参照

## サポート

- **Issues**: https://github.com/ShunsukeHayashi/Miyabi/issues
- **Discord**: [準備中]

## ライセンス

MIT

---

✨ [Miyabi](https://github.com/ShunsukeHayashi/Miyabi)で生成
